<Window x:Class="MifareReaderApp.Views.Dialogs.SelectItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Выберите элемент"
        Height="250"
        Width="350"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid FocusManager.FocusedElement="{Binding ElementName=ItemComboBox}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock"
                   Text="{Binding Message}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Stretch"
                   Margin="15 15 15 5"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   d:Text="Выберите значение из списка:" />

        <ComboBox x:Name="ItemComboBox"
                  Grid.Row="1"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Stretch"
                  Margin="25 0"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="{Binding DisplayMemberName}"
                  IsSynchronizedWithCurrentItem="True"
                  materialDesign:HintAssist.Hint="Элемент списка"
                  materialDesign:TextFieldAssist.HasClearButton="True" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0 5 0 15">
            <Button x:Name="OkButton"
                    Content="Подтвердить"
                    Width="130"
                    Margin="5,0"
                    IsDefault="True"
                    Click="OkButton_Click" />
            <Button x:Name="CancelButton"
                    Content="Отмена"
                    Width="100"
                    Margin="5,0"
                    IsCancel="True"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>